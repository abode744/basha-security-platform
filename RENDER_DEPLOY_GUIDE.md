# دليل تشغيل ورفع منصة BASHA على Render.com (مجاناً 100%)

يُعد **[Render.com](https://render.com)** الخيار السحابي الأفضل والأنسب لمنصة BASHA، لأنه يوفر خادماً حقيقياً مستمراً (24/7) دون قيود المهلة الزمنية المؤقتة مثل Vercel، مما يسمح للأدوات الـ 20 بإجراء الفحوصات الطويلة بهدوء واستقرار.

---

## الخطوات البسيطة للتشغيل على Render.com:

### الخطوة 1: رفع المشروع إلى GitHub (مرة واحدة)
1. افتح حسابك في **[GitHub.com](https://github.com)**.
2. اضغط على **`New repository`** وسمّه مثلاً: `basha-scanner`.
3. اضغط على **`uploading an existing file`** واسحب ملفات المشروع من جهازك (بعد فك ضغط `BASHA-v1-complete.zip`) ثم اضغط **Commit changes**.
*(تأكد من وجود مجلد `app`، ومجلد `static`، وملف `render.yaml` و `requirements.txt` في المستودع)*.

---

### الخطوة 2: الربط والتشغيل على Render.com
1. ادخل إلى **[dashboard.render.com](https://dashboard.render.com)** وسجل دخولك بحسابك في GitHub.
2. اضغط على الزر الأزرق في الأعلى: **`+ New`** واختر **`Web Service`**.
3. ستظهر لك قائمة بمستودعات GitHub الخاصة بك، اضغط على زر **`Connect`** بجانب مستودع `basha-scanner`.
4. ستجد الإعدادات مجهزة تلقائياً، وتأكد من الآتي:
   - **Name:** اكتب اسماً لموقعك (مثلاً: `basha-security`).
   - **Runtime:** `Python 3`.
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
   - **Instance Type:** اختر **Free** (مجاني 100%).
5. اضغط على الزر الأزرق بالأسفل: **`Deploy Web Service`**.

---

### الخطوة 3: استلام الرابط الحي
خلال دقيقة إلى دقيقتين، سيتولى Render تثبيت المكتبات وتشغيل الخادم، وسيعطيك رابطاً دائماً ومحمياً بشهادة SSL مجاناً مثل:
```text
https://basha-security.onrender.com
```

---

## بيانات تسجيل الدخول الافتراضية
- **اسم المستخدم:** `abod`
- **كلمة المرور:** `2024`

---

## مميزات تشغيل المنصة على Render:
- ✅ **فحوصات حقيقية دون انقطاع:** لا تتوقف عمليات الفحص في الخلفية.
- ✅ **جميع الأدوات الـ 20 تعمل بكفاءة:** بالاعتماد على المحرك الأمني المدمج فائق السرعة.
- ✅ **إصدار وتحميل التقارير:** يمكنك تحميل تقارير PDF و TXT و HTML فور انتهاء أي فحص.
- ✅ **رابط عالمي دائم ومجاني** يعمل من أي جهاز أو هاتف.
