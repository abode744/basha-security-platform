# دليل تشغيل ورفع منصة BASHA على Vercel

تم تجهيز المشروع بالكامل ليعمل على **[Vercel](https://vercel.com)** عبر ملف الإعداد `vercel.json` وبوابة `api/index.py`.

---

## الطريقة الأسهل: الرفع عبر GitHub إلى Vercel (موصى بها)

### الخطوة 1: رفع المشروع إلى GitHub
1. ادخل إلى حسابك في **[GitHub.com](https://github.com)**.
2. أنشئ مستودعاً جديداً (New Repository) وسمّه مثلاً: `basha-security`.
3. ارفع ملفات مشروع `BASHA-v1-complete` إلى المستودع (تأكد من وجود مجلد `app` ومجلد `static` وملف `vercel.json` و `requirements.txt` في جذر المستودع).

### الخطوة 2: الربط والتشغيل على Vercel
1. ادخل إلى حسابك في **[Vercel.com](https://vercel.com)**.
2. اضغط على **`Add New...`** ثم اختر **`Project`**.
3. ستظهر لك قائمة بمستودعات GitHub الخاصة بك، اضغط على زر **`Import`** بجانب مستودع `basha-security`.
4. اضغط مباشرة على الزر الأزرق **`Deploy`** دون الحاجة لتغيير أي إعدادات.
5. سيبدأ Vercel ببناء المشروع خلال دقيقة واحدة، وسيعطيك رابطاً مباشراً يعمل بشهادة أمان SSL مجاناً مثل:
   ```text
   https://basha-security.vercel.app
   ```

---

## الطريقة الثانية: الرفع المباشر عبر Vercel CLI
إذا كان لديك Node.js مثبت على جهازك:
1. افتح موجه الأوامر (CMD أو PowerShell) داخل مجلد المشروع:
   ```bash
   cd c:\Users\bwdy5\Downloads\BASHA-v1-complete
   ```
2. ثبّت أداة Vercel وسجّل دخولك:
   ```bash
   npm i -g vercel
   vercel login
   ```
3. ارفع المشروع فوراً بالأمر:
   ```bash
   vercel --prod
   ```

---

## بيانات تسجيل الدخول الافتراضية
- **اسم المستخدم:** `abod`
- **كلمة المرور:** `2024`

---

## ⚠️ تنبيه تقني هام حول Vercel ومقترح بديل ممتاز:
- **نظام Vercel:** يعمل بنظام **Serverless Functions** (دوال سحابية مؤقتة تنتهي في ثوانٍ). هذا ممتاز جداً لتصفح لوحة التحكم واستعراض التقارير. ولكن الفحوصات الطويلة جداً التي تستغرق دقائق قد تتوقف بسبب حد مهلة الدوال في Vercel المجاني (10-15 ثانية).
- **البديل السحابي المجاني الأفضل للفحص المستمر (Render.com):**
  إذا أردت خادماً سحابياً مجانياً يعمل 24/7 دون أي مهلة زمنية ويدعم استمرار الفحص في الخلفية، يمكنك استخدام **[Render.com](https://render.com)** بنفس مستودع GitHub وبضغطة زر واحدة (New Web Service).
